@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

<!DOCTYPE html>
<html lang="ar" dir="ltr">
<head>
    <meta charset="UTF-8" />
    <title>الأدوار</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@300;400;600;700&display=swap" rel="stylesheet" />
    <style>
        :root {
            --primary: #00b9f3;
            --primary-dark: #00a1d3;
            --bg-main: #f9fbfd;
            --bg-strip: #f4f6f8;
            --card-bg: #ffffff;
            --card-bottom: #e5f9ff;
            --border: #e4e9f2;
            --muted: #8f9bb3;
            --shadow-card: 0 10px 25px rgba(15, 23, 42, 0.08);
        }

        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: "Cairo", system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
            background: var(--bg-main);
            direction: rtl;
            color: #111827;
        }

        button {
            font-family: inherit;
        }

        .app {
            min-height: 100vh;
            display: flex;
            background: var(--bg-main);
        }

        .sidebar {
            width: 68px;
            background: var(--primary);
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: space-between;
            padding: 16px 0 20px;
            color: #ffffff;
        }

        .sidebar-top {
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 26px;
        }

        .sidebar-logo-wrapper {
            width: 42px;
            height: 42px;
            border-radius: 21px;
            border: 2px solid #ffffff;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 700;
            font-size: 20px;
        }

        .sidebar-icons {
            display: flex;
            flex-direction: column;
            gap: 14px;
        }

        .sidebar-icon-btn {
            width: 38px;
            height: 38px;
            border-radius: 19px;
            border: 1px solid rgba(255, 255, 255, 0.8);
            background: transparent;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            color: #ffffff;
            font-size: 18px;
            transition: background 0.18s, transform 0.06s;
        }

            .sidebar-icon-btn:hover {
                background: rgba(255, 255, 255, 0.18);
                transform: translateY(-1px);
            }

        .sidebar-bottom {
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 10px;
            color: #ffffff;
            font-size: 13px;
        }

        .sidebar-arrows {
            display: flex;
            gap: 6px;
        }

        .sidebar-arrow-btn {
            width: 22px;
            height: 22px;
            border-radius: 50%;
            border: 1px solid rgba(255, 255, 255, 0.9);
            background: transparent;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            font-size: 12px;
            color: #ffffff;
        }

        .main {
            flex: 1;
            display: flex;
            flex-direction: column;
            background: #ffffff;
        }

        .topbar {
            height: 64px;
            background: #ffffff;
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 0 26px;
            box-shadow: 0 1px 0 rgba(15, 23, 42, 0.04);
        }

        .topbar-right {
            display: flex;
            align-items: center;
            gap: 6px;
            font-size: 14px;
        }

        .breadcrumb-muted {
            color: var(--muted);
        }

        .breadcrumb-separator {
            color: var(--muted);
        }

        .breadcrumb-active {
            color: var(--primary);
            font-weight: 600;
        }

        .topbar-left {
            display: flex;
            align-items: center;
            gap: 14px;
            font-size: 14px;
            color: #4b5563;
        }

        .avatar {
            width: 32px;
            height: 32px;
            border-radius: 50%;
            background-image: url("https://i.pravatar.cc/64?img=5");
            background-size: cover;
            background-position: center;
        }

        .username {
            max-width: 110px;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        .lang-switch {
            padding: 3px 14px;
            border-radius: 16px;
            border: 1px solid var(--border);
            background: #f9fafb;
            font-size: 12px;
            font-weight: 600;
            cursor: pointer;
            color: #111827;
        }

        .bell-btn {
            width: 32px;
            height: 32px;
            border-radius: 50%;
            border: 1px solid var(--border);
            background: #ffffff;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            font-size: 16px;
            position: relative;
            color: #4b5563;
        }

        .bell-dot {
            position: absolute;
            top: 7px;
            left: 8px;
            width: 8px;
            height: 8px;
            border-radius: 50%;
            background: #f97373;
            border: 2px solid #ffffff;
        }

        .content {
            flex: 1;
            background: var(--bg-main);
            padding: 18px 26px 26px;
        }

        .strip {
            background: var(--bg-strip);
            padding: 10px 18px;
            border-radius: 4px;
            margin-bottom: 18px;
            display: flex;
            align-items: center;
            justify-content: flex-start;
            direction: ltr;
            background-color: #FFF;
        }

        .add-btn {
            padding: 7px 26px;
            border-radius: 4px;
            border: none;
            background: var(--primary);
            color: #ffffff;
            font-size: 14px;
            font-weight: 600;
            cursor: pointer;
            box-shadow: 0 8px 18px rgba(0, 185, 243, 0.35);
            transition: background 0.18s, transform 0.06s, box-shadow 0.18s;
        }

            .add-btn:hover {
                background: var(--primary-dark);
                transform: translateY(-1px);
                box-shadow: 0 10px 22px rgba(0, 185, 243, 0.38);
            }

        .roles-row {
            display: flex;
            flex-wrap: nowrap;
            gap: 18px;
            align-items: stretch;
        }

        .role-card {
            width: 260px;
            background: var(--card-bg);
            border-radius: 18px;
            box-shadow: var(--shadow-card);
            overflow: hidden;
            display: flex;
            flex-direction: column;
        }

        .role-card-inner {
            padding: 14px 18px 16px;
            display: flex;
            flex-direction: column;
            gap: 12px;
        }

        .role-card-top {
            display: flex;
            align-items: center;
            justify-content: space-between;
            direction: ltr;
        }

        .ellipsis-btn {
            width: 28px;
            height: 28px;
            border-radius: 50%;
            border: 1px solid var(--border);
            background: #ffffff;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            font-size: 16px;
            color: #6b7280;
            box-shadow: 0 4px 10px rgba(15, 23, 42, 0.08);
        }

        .role-icon {
            width: 40px;
            height: 40px;
            border-radius: 20px;
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
            margin-inline-start: 6px;
        }

        .role-icon-head {
            position: absolute;
            top: 3px;
            left: 50%;
            transform:scale() translateX(-50%);
            width: 14px;
            height: 14px;
            border-radius: 50%;
            border: 2px solid var(--primary);
            background: #ffffff;
        }

        .role-icon-body {
            width: 22px;
            height: 10px;
            border-radius: 20px 20px 8px 8px;
            border: 2px solid #111827;
            border-top: none;
            position: absolute;
            bottom: 7px;
            left: 50%;
            transform: translateX(-50%);
        }

        .role-title {
            text-align: center;
            font-size: 16px;
            font-weight: 600;
            margin-top: 6px;
        }

        .role-bottom {
            background: var(--card-bottom);
            padding: 8px 18px;
            font-size: 13px;
            color: #000000;
            text-align: left;
            font: bold;
        }

        .role-users-count {
            color: #000000;
            font-size: 12px;
            font-weight: bold;
        }

        media (max-width: 960px) {
            .sidebar {
                display: none;
            }

            .topbar {
                padding-inline: 16px;
            }

            .content {
                padding-inline: 16px;
            }

            .roles-row {
                flex-wrap: wrap;
            }

            .role-card {
                width: calc(50% - 10px);
            }
        }

        media (max-width: 640px) {
            .role-card {
                width: 100%;
            }

            .roles-row {
                gap: 12px;
            }

            .username {
                display: none;
            }
        }
    </style>
</head>
<body>
    <div class="app">
        <aside class="sidebar">
            <div class="sidebar-top">
                <div class="sidebar-logo-wrapper">
                    <span>o</span>
                </div>
                <div class="sidebar-icons">
                    <button class="sidebar-icon-btn">📊</button>
                    <button class="sidebar-icon-btn">📈</button>
                    <button class="sidebar-icon-btn">📃</button>
                    <button class="sidebar-icon-btn">📁</button>
                    <button class="sidebar-icon-btn">📅</button>
                </div>
            </div>
            <div class="sidebar-bottom">
                <div class="sidebar-arrows">
                    <button class="sidebar-arrow-btn">◀</button>
                    <button class="sidebar-arrow-btn">▶</button>
                </div>
            </div>
        </aside>

        <div class="main">
            <header class="topbar">
                <div class="topbar-right">
                    <span class="breadcrumb-muted">إدارة</span>
                    <span class="breadcrumb-separator">-</span>
                    <span class="breadcrumb-active">الأدوار</span>
                </div>
                <div class="topbar-left">
                    <div class="avatar"></div>
                    <span class="username">عمر ..</span>
                    <button class="lang-switch">EN</button>
                    <button class="bell-btn">
                        <span class="bell-dot"></span>
                        🔔
                    </button>
                </div>
            </header>

            <main class="content">
                <div class="strip">
                    <button class="add-btn">إضافة</button>
                </div>

                <section class="roles-row">
                    <article class="role-card">
                        <div class="role-card-inner">
                            <div class="role-card-top">
                                <button class="ellipsis-btn">⋯</button>
                            </div>
                            <div class="role-icon">
                                <div class="role-icon-head"></div>
                                <div class="role-icon-body"></div>
                            </div>
                            <h3 class="role-title">مالك المطعم</h3>
                        </div>
                        <div class="role-bottom">
                            <span class="role-users-count">5 مستخدمين</span>
                        </div>
                    </article>

                    <article class="role-card">
                        <div class="role-card-inner">
                            <div class="role-card-top">
                                <button class="ellipsis-btn">⋯</button>
                            </div>
                            <div class="role-icon">
                                <div class="role-icon-head"></div>
                                <div class="role-icon-body"></div>
                            </div>
                            <h3 class="role-title">مدير الفرع</h3>
                        </div>
                        <div class="role-bottom">
                            <span class="role-users-count">5 مستخدمين</span>
                        </div>
                    </article>

                    <article class="role-card">
                        <div class="role-card-inner">
                            <div class="role-card-top">
                                <button class="ellipsis-btn">⋯</button>
                            </div>
                            <div class="role-icon">
                                <div class="role-icon-head"></div>
                                <div class="role-icon-body"></div>
                            </div>
                            <h3 class="role-title">كاشير</h3>
                        </div>
                        <div class="role-bottom">
                            <span class="role-users-count">5 مستخدمين</span>
                        </div>
                    </article>
                </section>
            </main>
        </div>
    </div>
</body>
</html>